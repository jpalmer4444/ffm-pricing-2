<?php
$this->headTitle('Users');

$this->mainMenu()->setActiveItemId('users');

$this->mainMenu()->setSiteMapOverride('Manage Users');

$this->pageBreadcrumbs()->setItems([
    'Home' => $this->url('home'),
    'Manage Users' => $this->url('users'),
]);
?>

<div class="around_datatables" ng-controller="UsersTableController as user">

    <?= $this->partial('partial/users-table-header-tplt') ?>

    <div >
        <table datatable="usersTable" dt-instance="user.dtInstanceCallback" dt-options="user.dtOptions" dt-columns="user.dtColumns" style="width: 100%;" class="table-bordered table-condensed table-hover table-striped">
            <thead>
                <tr class="no-padding">
                    <th>
                        <!-- ID -->
                    </th>
                    <th>
                        <!-- USERNAME -->
                        <input name="zff_username" ffm-enter="user.reloadData()" ng-model="user.zff_username" class="filter form-control" value="" type="text">
                    </th>
                    <th>
                        <!-- EMAIL -->
                        <input name="zff_email" ffm-enter="user.reloadData()" ng-model="user.zff_email" class="filter form-control" value="" type="text">
                    </th>
                    <th>
                        <div class="btn-group bootstrap-select filter form-control" uib-dropdown dropdown-append-to-body="true">
                            <ul class="dropdown-menu table-head" uib-dropdown-menu >
                                <li >
                                    <a href="#" ng-click="user.selectStatus('Active')">Active</a>
                                </li>
                                <li >
                                    <a href="#" ng-click="user.selectStatus('Inactive')">Inactive</a>
                                </li>
                            </ul>
                            <button id="single-button" type="button" class="btn btn-default" uib-dropdown-toggle >
                                <span ng-if="!user.status" class="filter-option pull-left">- Select -</span>
                                <span ng-if="user.status" class="filter-option pull-left">{{user.status}}</span>
                                &nbsp;
                                <span class="caret"></span>
                            </button>

                        </div>
                        <!-- STATUS -->

                    </th>
                    <th>
                        <!-- FULL NAME -->
                        <input name="zff_fullname" ffm-enter="user.reloadData()" ng-model="user.zff_fullname" class="filter form-control" value="" type="text">
                    </th>
                    <th>
                        <!-- CREATED -->
                        <label ng-click="user.zff_createddate_open = true" class="cursor-pointer date-picker-table">
                            <input name="zff_createdDate" ng-change="user.reloadData()" readonly datepicker-append-to-body="true" type="text" class="form_datetime filter form-control" uib-datepicker-popup="{{user.dateformat}}" ng-model="user.zff_createddate" is-open="user.zff_createddate_open" datepicker-options="user.dateOptions" alt-input-formats="user.altInputFormats" />
                            <i class="ion-calendar" ></i>
                        </label>
                    </th>
                    <th>
                        <!-- LAST LOGIN -->
                        <label ng-click="user.zff_lastlogindate_open = true" class="cursor-pointer date-picker-table">
                            <input name="zff_lastLogin" ng-change="user.reloadData()" readonly datepicker-append-to-body="true" type="text" class="form_datetime filter form-control" uib-datepicker-popup="{{user.dateformat}}" ng-model="user.zff_lastlogindate" is-open="user.zff_lastlogindate_open" datepicker-options="user.dateOptions" alt-input-formats="user.altInputFormats" />
                            <i class="ion-calendar" ></i>
                        </label>
                    </th>
                    <th>
                        <!-- ACTIONS -->
                    </th>
                </tr>
                <tr>
                    <th>ID</th>
                    <th>Username</th>
                    <th>Email</th>
                    <th>Status</th>
                    <th>Fullname</th>
                    <th>Created</th>
                    <th>Last Login</th>
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody></tbody>
            <tfoot>
                <tr>
                    <th>ID</th>
                    <th>Username</th>
                    <th>Email</th>
                    <th>Status</th>
                    <th>Fullname</th>
                    <th>Created</th>
                    <th>Last Login</th>
                    <th>Actions</th>
                </tr>
            </tfoot>
        </table>
    </div>
</div>




